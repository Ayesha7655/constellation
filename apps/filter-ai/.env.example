# Filter AI microservice — generates Apify Upwork search filters via ChatGPT
PORT=4060

# Shared secret; Nest must send the same value as x-internal-key
FILTER_AI_INTERNAL_KEY=dev-filter-ai-internal-key

# OpenAI — replace with a real key
OPENAI_API_KEY=sk-example-replace-me
OPENAI_MODEL=gpt-4o-mini
